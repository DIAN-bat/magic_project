import 'package:flutter/material.dart';

class Apptheme {
  static final primaryColor = Colors.amber;

  static final darkTheme = ThemeData.dark().copyWith(
    primaryColor: primaryColor,
    
  );
}
